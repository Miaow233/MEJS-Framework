{
  "compilerOptions": {
    "diagnostics": true, // 打印编译信息

    "target": "ES6", // 目标语言的版本
    //"outFile": "./dist/app.js", // 将多个相互依赖的文件生成一个文件

    "lib": ["ES6"], // TS 需要引用的库

    "allowJs": true, // 允许编译 JS 文件
    "checkJs": true, // 允许在 JS 文件中报错，通常与 allowJS 一起使用
    "outDir": "./dist", // 指定输出目录
    "module": "ES6", // 模块类型

    "esModuleInterop": true // 允许 export = 导出，由import from 导入
  },
  "include": ["./src/**/*"],
  "exclude": ["./src/test/**/*"]
}
